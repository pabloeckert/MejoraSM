# project_id confirmado directamente por Pablo el 2026-07-28 (URL, publishable
# key y comando `supabase link --project-ref hsglmdarztrshihmzfph`).
#
# Nota: un intento anterior de "confirmar" esto leyendo el bundle ya
# deployado en producción encontró exnjyxwmxknvzploeaex — pero ese bundle
# estaba compilado con un secret VITE_SUPABASE_URL de GitHub Actions que
# apuntaba a un proyecto viejo/abandonado, no al que Pablo usa hoy. La fuente
# de verdad es la confirmación directa, no lo que estuviera deployado.
project_id = "hsglmdarztrshihmzfph"
